{
  "name": "base64-reduce",
  "description": "Decode Base64 images and try shorter data URIs, entirely in the browser.",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "author": "davlgd (https://davlgd.fr)",
  "homepage": "https://base64.davlgd.com",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davlgd/base64-reduce.git"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun src/server.ts",
    "dev": "DEV=1 bun --watch src/server.ts",
    "typecheck": "tsc && tsc -p src/client",
    "test": "bun test",
    "check": "bun run typecheck && bun test"
  },
  "engines": {
    "bun": ">=1.3"
  },
  "dependencies": {
    "@fontsource-variable/atkinson-hyperlegible-mono": "^5.3.0",
    "@fontsource-variable/atkinson-hyperlegible-next": "^5.3.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.2",
    "typescript": "^7.0.2"
  }
}
